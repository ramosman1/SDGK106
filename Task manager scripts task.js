///// this can be use anywhere
// this is what is important and in OBJ
// the names here are what matter
class Task {
  constructor(id, title, isImportance, duration, deadline, location, status) {
    this.id = id;
    this.title = title;
    this.isImportant = isImportance;
    this.duration = duration;
    this.deadline = deadline;
    this.location = location;
    this.status = status;

    this.name = "Man";
  }
}